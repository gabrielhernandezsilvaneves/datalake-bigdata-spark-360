package com.enterprise.visao360.lake.utils;

/**
 * 
 * Classe responsável por fazer o parse de um Result para a classe Manager.
 * 
 * **/
public class Parse {

	/**
	 * 
	 * Metodo para fazer o parse de um Result para a classe Manager.
	 * 
	 **/
/*	public Manager parseManager(Result res) {
		final Manager manager = new Manager();
		manager.setCodigoUnidade(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.CODIGO_UNIDADE.getBytes())));
		manager.setMatriculaGerente(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.MATRICULA_GERENTE.getBytes())));
		manager.setPerfilGerente(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.PERFIL_GERENTE.getBytes())));
		manager.setTipoUnidade(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.TIPO_UNIDADE.getBytes())));
		manager.setSiglaGerente(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.SIGLA_GERENTE.getBytes())));						 
		manager.setNome(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.NOME_GERENTE.getBytes())));
		manager.setGrupoPerfil(Bytes.toString(res.getValue(VISAO360LakeConstantes.COLUMN_FAMILY_NAME_G.getBytes(), VISAO360LakeConstantes.GRUPO_PERFIL.getBytes())));
		return manager;
	}*/
		
}
